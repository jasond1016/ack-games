# PROTOTYPE NOTES — Racing pre-start UI (v2)

**Question:** Pre-start UI — visual-heavy, light text, gamepad-friendly, not kiddy; Motorfest-inspired.

**Owner pick:** **B — Horizon Strip** → implemented in real racing start overlay (first slice).

**Gates:** U0–U4 finalized by @hReviewer.

**First slice (done):** start overlay (car + coastal mode + GO) — Horizon Strip in `index.html` / `styles.css` / `racing-game.js`.

**Second slice (optional):** map select page same style — not started unless owner asks.

**Artifact kept for reference:** `prototype-racing-prestart.html?variant=B`
